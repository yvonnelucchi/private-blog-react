import React from 'react';


export default function blogpostPagina() {
    return(
        <>
        <h2>blogpostPagina</h2>
        <div>De Blogpost pagina is een component dat op basis van de parameter in de url de juiste blogpost ophaalt uit de JSON data (deze is bijgeleverd), en deze weergeeft (dynamic parameters). De JSON data kun je simpelweg importeren en gebruiken als een array met objecten:</div>

        </>
    );
}